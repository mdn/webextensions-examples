{
  "name": "Keyboard Pin (WebExtension/BrowserExt API version)",
  "version": "0.4",
  "description": "Creates a keyboard shortcut (Alt + Shift + P) to toggle the pinned state of the currently selected tab using the WebExtension/BrowserExt API",
  "background": {
    "persistent": false,
    "scripts": [
        "browser-polyfill.js",
        "background.js"
    ]
  },
  "commands": {
    "toggle-pin": {
      "suggested_key": { "default": "Alt+Shift+P" },
      "description": "Toggle tab pin"
    }
  },
  "manifest_version": 2
}
